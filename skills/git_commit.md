# git commit スキル

1. `run_git(["status"])` を呼ぶ
2. `nothing to commit` なら「変更なし」と表示して終了
3. `run_git(["commit", "-m", "日本語のメッセージ"])` を呼ぶ。**`-m` と メッセージは必須**。`run_git(["commit"])` だけは禁止。
4. 結果をそのまま表示する。

ツールを呼ばずに説明するだけは禁止。
